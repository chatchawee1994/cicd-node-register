name: CI

on:
  push:
    branches: [main]

jobs:
  test:
    runs-on: self-hosted

    steps:
      - uses: actions/checkout@v4
      - run: node -v
      - run: npm -v
      - run: npm install
      - run: npm test