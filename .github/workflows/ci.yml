name: CI

on:
  push:
    branches: [main]

jobs:
  test:
    runs-on: self-hosted

    steps:
      - uses: actions/checkout@v4

      - name: Show Node version
        run: node -v

      - name: Install dependencies
        run: npm install

      - name: Run tests
        run: npm test